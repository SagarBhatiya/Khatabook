<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit Hisaab - Khaatabook</title>
    <link rel="stylesheet" href="/styles.css" />
    <!-- External CSS -->
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 20px;
      }
      .navbar {
        display: flex;
        justify-content: flex-end;
        gap: 20px;
      }
      .container {
        margin-top: 20px;
      }
      a {
        color: black;
      }
      .date {
        color: #007bff;
        text-decoration: underline;
      }
      textarea {
        width: 100%;
        height: 150px;
        padding: 10px;
        font-size: 16px;
        border: 1px solid #ccc;
        border-radius: 5px;
        resize: none;
      }
      .btn {
        background-color: #007bff;
        color: white;
        padding: 10px 15px;
        border: none;
        cursor: pointer;
        margin-top: 10px;
        border-radius: 5px;
      }
      .btn:hover {
        background-color: darkblue;
      }
    </style>
  </head>
  <body>
    <h1>Khaatabook</h1>
    <div class="navbar">
      <a href="/">Home</a>
      <a href="/create">Create New Hisaab</a>
    </div>

    <div class="container">
      <h2>
        You're editing hisaab of
        <span class="date"><%= filename.split(".")[0] %></span> date
      </h2>

      <form action="/update/<%= filename %>" method="POST">
        <textarea name="hisaab" placeholder="Write your hisaab">
<%=data%></textarea
        >
        <button type="submit" class="btn">Update Hisaab</button>
      </form>
    </div>
  </body>
</html>
